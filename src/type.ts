export interface FiberDetil {
  name: string;
  location: string;
}
export interface CameraDetil {
  name: string;
  location: string;
}
export interface User {
  name: string;
  type: number;
  password: string;
}
